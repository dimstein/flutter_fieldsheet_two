import 'package:flutter/material.dart';

class SimpleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Simple Screen'),
      ),
    );
  }
}
